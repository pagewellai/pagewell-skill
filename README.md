# pagewell-skill
 Agent skill + CLI for PageWell: write a designed document or a runnable single-page artifact with your AI agent, then publish it as a link or a share code. Prebuilt binaries for macOS / Linux / Windows.
